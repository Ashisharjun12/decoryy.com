/** Min time between successful Ola direction fetches while en route. */
export const ROUTE_REFETCH_MIN_MS = 15_000;

/** Refetch when worker moves at least this far since last route origin. */
export const ROUTE_REFETCH_MOVE_M = 75;

export const ROUTE_RETRY_EMPTY_MS = 15_000;
