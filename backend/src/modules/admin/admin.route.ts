import { Router } from "express";

/**
 * Admin surface: mount catalog/geo/booking/... admin routes here.
 * This module has no tables. See docs/project-requriment.md
 */
export const adminRouter = Router();
